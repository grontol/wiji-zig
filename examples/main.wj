struct Thing {
    x: i32
    
    fn foo(self: @Self) {
        
    }
}

impl string {
    @builtin @readonly ptr: *u8
    @builtin @readonly len: usize
    
    fn split(self: @Self): []@Self {
        
    }
}