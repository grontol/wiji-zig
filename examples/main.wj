var F = Foo.FOOD
const X = Y + 10
const Y = 20 + Z// + Foo.FOOD
const Z = 40

const FOO = Foo.{10}

struct Foo {
    const FOOD = 30
    
    x: i32
    
    fn print(self: @Self) {
        printf("x = %d\n", self.x)
    }
}

fn main() {
    printf("X = %d\n", X)
}

extern fn printf(fmt: string, ...args: any)