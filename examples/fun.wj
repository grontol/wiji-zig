import "std"

fn main() {
    const input = .[2, 5, 1, 8, 4, 3, 9, 6]
    
    for i in 0..input.len {
        if i < input.len - 1 {
            const pair = .[input[i] + input[i + 1], input[i + 1]]
            print("Pair : {}\n", pair)
        }
    }
}